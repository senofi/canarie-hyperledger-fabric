name: TEST

on:
  push:
    branches:
      - 'main'
      - 'develop'
      - 'v[0-9]*'
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  vagrant-up:
    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2

    - name: Cache Vagrant boxes
      uses: actions/cache@v2
      with:
        path: ~/.vagrant.d/boxes
        key: ${{ runner.os }}-vagrant-${{ hashFiles('Vagrantfile') }}
        restore-keys: |
          ${{ runner.os }}-vagrant-

    - name: Run vagrant up
      run: BRANCH=${GITHUB_REF#refs/heads/} vagrant up

    - name: Find containers that may not be started up
      id: docker-containers-not-up
      run: echo '::set-output name=CONTAINERS_COUNT::$(vagrant ssh -c "sudo docker ps | grep -v ' Up ' | wc -l")'

    - name: Validate containers count
      if: ${{ steps.docker-containers-not-up.outputs.CONTAINERS_COUNT }} > 0
      run: exit 1
